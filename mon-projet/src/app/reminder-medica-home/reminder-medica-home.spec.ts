/** Fichier réservé : ajouter des tests quand le module aura une implémentation stable. */
describe('reminder-medica-home (placeholder)', () => {
  it('suite chargée', () => {
    expect(true).toBe(true);
  });
});
