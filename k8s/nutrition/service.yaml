apiVersion: v1
kind: Service
metadata:
  name: nutrition
  labels:
    app.kubernetes.io/name: nutrition
spec:
  type: ClusterIP
  ports:
    - port: 8088
      targetPort: 8088
      name: http
  selector:
    app.kubernetes.io/name: nutrition
