apiVersion: v1
kind: Service
metadata:
  name: parametrevital
  labels:
    app.kubernetes.io/name: parametrevital
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  selector:
    app.kubernetes.io/name: parametrevital
