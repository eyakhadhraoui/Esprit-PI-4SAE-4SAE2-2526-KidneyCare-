apiVersion: v1
kind: Service
metadata:
  name: infection-vaccination
  labels:
    app.kubernetes.io/name: infection-vaccination
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      name: http
  selector:
    app.kubernetes.io/name: infection-vaccination
