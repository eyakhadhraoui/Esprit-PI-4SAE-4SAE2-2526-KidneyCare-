apiVersion: v1
kind: Service
metadata:
  name: consultation
  labels:
    app.kubernetes.io/name: consultation
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  selector:
    app.kubernetes.io/name: consultation
