apiVersion: v1
kind: Service
metadata:
  name: eureka
  labels:
    app.kubernetes.io/name: eureka
spec:
  type: ClusterIP
  ports:
    - port: 8761
      targetPort: 8761
      name: http
  selector:
    app.kubernetes.io/name: eureka
