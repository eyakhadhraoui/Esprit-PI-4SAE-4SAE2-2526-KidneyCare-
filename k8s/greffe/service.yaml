apiVersion: v1
kind: Service
metadata:
  name: greffe
  labels:
    app.kubernetes.io/name: greffe
spec:
  type: ClusterIP
  ports:
    - port: 8096
      targetPort: 8096
      name: http
  selector:
    app.kubernetes.io/name: greffe
