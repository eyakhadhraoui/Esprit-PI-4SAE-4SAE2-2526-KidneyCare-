apiVersion: v1
kind: Service
metadata:
  name: nephro
  labels:
    app.kubernetes.io/name: nephro
spec:
  type: ClusterIP
  ports:
    - port: 8089
      targetPort: 8089
      name: http
  selector:
    app.kubernetes.io/name: nephro
